<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/sec_header"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="8dp"
    android:paddingRight="8dp"
    android:paddingBottom="8dp"
    >

    <TextView
        android:id="@+id/sec_header_title"
        style="@style/ListHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:focusable="false"
        android:gravity="left"
        android:maxLines="1"
        android:textColor="@color/list_header_text"
        app:layout_constraintBaseline_toBaselineOf="@+id/sec_action_button"
        app:layout_constraintEnd_toStartOf="@+id/sec_action_button"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Recently updated" />

    <Button
        android:id="@+id/sec_action_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|right"
        android:text="@string/update_all"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/sec_header_count"
        app:layout_constraintStart_toEndOf="@+id/sec_header_title" />

    <TextView
        android:id="@+id/sec_header_count"
        style="@style/ListHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="right"
        android:textColor="@color/list_header_text"
        app:layout_constraintBaseline_toBaselineOf="@+id/sec_action_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/sec_action_button"
        tools:text="156" />
</android.support.constraint.ConstraintLayout>